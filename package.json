"homepage": "git@github.com:gacoelho/Manrio_SPA_Site.git,
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}

